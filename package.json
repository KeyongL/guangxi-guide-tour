{
  "name": "guangxi-guide",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "check:compliance": "node scripts/check-compliance.mjs",
    "prebuild": "npm run check:compliance",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5"
  }
}
